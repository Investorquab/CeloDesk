name: Deploy CeloDesk Backend

on:
  push:
    branches: [main]
    paths:
      - 'backend/**'
      - '.github/workflows/deploy-backend.yml'
  workflow_dispatch:

concurrency:
  group: celodesk-backend-production
  cancel-in-progress: false

jobs:
  build:
    name: Build backend
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: backend/package-lock.json

      - name: Install dependencies
        working-directory: backend
        run: npm ci

      - name: Build backend
        working-directory: backend
        run: npm run build

  deploy:
    name: Deploy to VPS
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup SSH
        env:
          VPS_SSH_KEY_B64: ${{ secrets.VPS_SSH_KEY_B64 }}
        run: |
          set -euo pipefail
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh
          python3 - <<'PY'
          import base64, os
          value = os.environ["VPS_SSH_KEY_B64"]
          value = "".join(value.split())
          value += "=" * ((4 - len(value) % 4) % 4)
          key = base64.b64decode(value, validate=True)
          with open(os.path.expanduser("~/.ssh/celodesk_deploy"), "wb") as f:
              f.write(key)
          PY
          chmod 600 ~/.ssh/celodesk_deploy
          ssh-keygen -y -f ~/.ssh/celodesk_deploy > ~/.ssh/celodesk_deploy.pub
          ssh-add ~/.ssh/celodesk_deploy

      - name: Trust VPS host key
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -H "${{ secrets.VPS_HOST }}" >> ~/.ssh/known_hosts

      - name: Verify VPS SSH access
        run: |
          ssh -i ~/.ssh/celodesk_deploy -o IdentitiesOnly=yes -o BatchMode=yes "${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }}" 'echo "SSH connection OK"'

      - name: Upload backend source
        run: |
          rsync -az --exclude='.env' --exclude='node_modules' --exclude='dist' \
            -e 'ssh -i ~/.ssh/celodesk_deploy -o IdentitiesOnly=yes' \
            backend/ \
            "${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }}:/root/celodesk/backend/"

      - name: Build and restart production API
        run: |
          ssh -i ~/.ssh/celodesk_deploy -o IdentitiesOnly=yes "${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }}" 'bash -s' <<'REMOTE'
          set -e
          cd /root/celodesk/backend
          npm ci
          npm run build
          pm2 restart celodesk-api --update-env
          sleep 3
          curl --fail --silent --show-error https://api.185-7-81-139.sslip.io/health
          echo
          echo "CeloDesk backend deployment successful."
          REMOTE
