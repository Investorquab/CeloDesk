import {InvoiceStatus} from '../lib/api'; export function StatusBadge({status}:{status:InvoiceStatus}){const c=status==='PAID'||status==='OVERPAID'?'paid':status==='PENDING'?'pending':status==='OVERDUE'||status==='FAILED'||status==='EXPIRED'?'overdue':status==='SENT'||status==='VIEWED'||status==='PARTIALLY_PAID'?'sent':'draft'; return <span className={`pill ${c}`}>{status.replace('_',' ')}</span>}
